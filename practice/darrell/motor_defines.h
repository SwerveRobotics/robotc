#ifndef MOTOR_DEFINES_H
#define MOTOR_DEFINES_H

const int MAX_MOTOR_SPEED = 100;// maximum speed we should intentionally give the motor
const int MIN_MOTOR_SPEED = 20; // minimum speed at which motor will move













#endif
