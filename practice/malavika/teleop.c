#pragma config(Sensor, S1,     colorSens,      sensorI2CHiTechnicColor)
#pragma config(Motor,  motorA,          left,          tmotorNXT, openLoop, reversed, encoder)
#pragma config(Motor,  motorB,          right,         tmotorNXT, openLoop, reversed, encoder)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#pragma debuggerWindows("nxtLCDScreen");
//#pragma debuggerWindows("Servos");
//#pragma debuggerWindows("joystickSimple");

#include "controller.c"
#include "arcade.c"
#include "colors.c"
#include "sensors.c"

task main()
{
	StartTask(drive);
	StartTask(controller);
	StartTask(sensors);

	IntializeColors();

	while(true)
	{
		// don't stop the task
	}
}
