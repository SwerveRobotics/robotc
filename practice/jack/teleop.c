#pragma config(Motor,  motorA,          left,          tmotorNXT, openLoop, encoder)
#pragma config(Motor,  motorB,          right,         tmotorNXT, PIDControl, encoder)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#pragma debuggerWindows("nxtLCDScreen");
//#pragma debuggerWindows("Servos");
#pragma debuggerWindows("joystickSimple");

#include "JoystickDriver.c"
#include "motors.c"
int deadZone = 15;
//int l = 0;
//int ml = 0;
//int r = 0;
//int mr = 0;

task main();
{
		while(true)
		{
		//	getJoystickSettings(joystick);
			//l = joystick.joy1_y1;
			//r = joystick.joy1_y2;
			if(abs(joystick.joy1_y1) > deadZone)
			{
				SetLeftMotorSpeed(joystick.joy1_y1);
			}
			else
			{
				SetLeftMotorSpeed(0);
			}
			if(abs(joystick.joy1_y2) > deadZone)
			{
				SetRightMotorSpeed(joystick.joy1_y2);
			}
			else
			{
				SetRightMotorSpeed(0);
			}
		}
}
